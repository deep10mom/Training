let abc =100;
if (abc==100)
{
    var abc=200;
    console.log(abc);//200 if we use let then scopes is restricted.. abc is under this block .
}
console.log(abc);
var tuple:any[]=[122,"abc",[1,2,3],{id:1,value:7654}];
for (let index = 0; index < tuple.length; index++) {
    const element = tuple[index];
    switch (tuple[index[index]]) {
        case object:
            
            break;
        case 
        default:
            break;
    }
    
}
var item:number=<number>tuple[index];

function show(name:string|string[]){//Union
    if(typeof name=="string")
    {

    }
}